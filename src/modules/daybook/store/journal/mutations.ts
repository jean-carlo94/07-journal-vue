import type { State } from "./state";

export const setEntry = ( state:State ) => {
    
}

export const createEntry = ( state:State ) => {
    
}

export const updateEntry = ( state:State ) => {
    
}