import type { ActionContext } from "vuex";
import type { State } from "./state";


export const loadEntries = async ({ commit } : ActionContext<State, State>) => {
    
}

export const createEntries = async ({ commit } : ActionContext<State, State>) => {
    
}

export const updateEntries = async ({ commit } : ActionContext<State, State>) => {
    
}
